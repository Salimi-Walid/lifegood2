import 'package:flutter/material.dart';

class ShowInputdata extends StatelessWidget {
  const ShowInputdata({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}