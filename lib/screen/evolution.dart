import 'package:flutter/material.dart';

class Evolution extends StatelessWidget {
  const Evolution({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(actions: [Text('data')]));
  }
}
